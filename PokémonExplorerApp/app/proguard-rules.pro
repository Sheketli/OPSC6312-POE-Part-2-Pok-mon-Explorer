# Keep default Android optimisations. Nothing app-specific is required yet,
# but this file is referenced by the release build type.
#
# If you enable `isMinifyEnabled = true`, note that the PokeAPI client parses
# responses by hand with org.json (no reflection), so no extra keep rules are
# needed for serialisation.
